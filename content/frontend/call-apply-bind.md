---
title: "call, apply, and bind"
category: "JavaScript"
description: "Explicitly setting this context and borrowing methods."
---

# call, apply, and bind


### Comparison

- **`call(thisArg, arg1, arg2, ...)`**: Invokes the function immediately with comma-separated arguments.
- **`apply(thisArg, [argsArray])`**: Invokes the function immediately with arguments passed as an array.
- **`bind(thisArg, arg1, ...)`**: Returns a **new bound function** with permanently preset `this` and optional preset arguments (currying).

